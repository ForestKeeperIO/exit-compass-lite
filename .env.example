AMBIGUOUS_API_KEY=
OPENAI_API_KEY=

# Optional: where the synthetic seed messages should be delivered.
# Leave this on a non-deliverable example.com address for the demo.
AMBIGUOUS_INBOX_ADDRESS=exit-compass@example.com
